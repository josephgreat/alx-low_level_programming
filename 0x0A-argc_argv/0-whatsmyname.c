#include <stdio.h>
#include <stdlib.h>

/**
 * main - name of file
 * @argc: argument count
 * @argv: argument array
 * Return: 0
 */

int main(int __attribute__((__unused__)) argc, char **argv)
{
	printf("%s\n", argv[0]);
	return (0);
}
