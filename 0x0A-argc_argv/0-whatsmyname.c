#include "main.h"
#include <stdio.h>

/**
 * main - print the name of the file
 * @argc: argument count
 * @argv: argument array
 * Return: always 0
 */

int main(int __attribute__((unused)) argc, char **argv)
{
	printf("%s\n", argv[0]);
	return (0);
}
