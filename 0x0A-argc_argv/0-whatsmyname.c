#include "main.h"
#include <stdio.h>

/**
 * main - name of file
 * @argc: argument count
 * @argv: argument array
 * Return: 0
 */

int main(int __attribute__((unused)) argc, char **argv)
{
	printf("%s\n", argv[0]);
	return (0);
}
